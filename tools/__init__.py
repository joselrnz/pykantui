"""Dev scripts: gif and screenshot rendering, live-run helpers, secret scan.

Not shipped in the wheel. The ``__init__`` exists so mypy checks the scripts
under one stable module name each; every script still runs directly with
``python tools/<name>.py``.
"""
